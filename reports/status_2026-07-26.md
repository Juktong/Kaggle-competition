# Status — 2026-07-26

Live-refreshed 02:13 UTC. Deadline **2026-08-05 23:59 UTC** (~10.9 days). Quota **0/5 today**
(`54968060` counted against 2026-07-25, 1/5 there). No jobs running; no stale watchers.

## Board

| ref | public | side | role |
|---|---|---|---|
| `54922806` | **6.563** | teammate | best-public (frontier, overlap-ON) — **slot 1** |
| **`54968060`** | **6.643** | **ours** | overlap-OFF frontier — **live slot-2 candidate** |
| `54896975` | 6.669 | teammate | frontier overlap-ON |
| `54923144` | 6.678 | teammate | frontier GR-sigma branch |
| `54844628` | 7.891 | ours | fully-owned honest slot — slot-2 fallback |
| `54878409` | 7.953 | ours | excluded (regression) |

## The one result that changed the picture

`54968060` (frontier with `guarded_overlap_override` FORCED OFF) scored **6.643**:

```
7.891 (our honest) -> 6.643 (frontier, NO overlap) = 1.248   frontier MODELING
6.643 -> 6.563 (best overlap-ON branch)            = 0.080   overlap + config, within the ~0.115 noise floor
```

**The overlap lookup is not what makes the 6.5 line strong.** The earlier hypothesis (drawn from the
mechanism being large in ft — 100% toe override, ~3.2 ft movement) is corrected: it is small in score.
`54968060` even beats two overlap-ON family members (6.669, 6.678).

Consequence: the frontier family's advantage does **not** depend on the test wells being duplicated in
train, so its expected value on novel wells (H-hidden) is much higher than previously assessed.

## Final-2 recommendation

- **Slot 1: `54922806` (6.563)** — best measured public, unchanged.
- **Slot 2: `54968060` (6.643) preferred on evidence; `54844628` (7.891) is the provenance-first
  fallback.** `54968060` is better by 1.248 on public, better locally (3.259 vs 3.677 RMSE), and is a
  *direct* measurement of the no-overlap/novel-well path. Its caveat is lineage: it derives from the
  public Kaiwalya notebook plus 9 third-party public datasets and has no 760-well OOF validation, while
  `54844628` is fully owned and OOF-validated. Choose `54844628` only under a provenance-first policy.

Full reasoning: `reports/final_slot_package_corrected_gate_2026-07-25.md`.

## Next

Nothing is blocked and no run is pending. Remaining useful work, in order:
1. If the final-selection owner wants the frontier line represented by a *fully-owned* candidate, the
   next step is an our-account overlap-OFF **full-fidelity variant** with an OOF-style local validation —
   this narrows the provenance gap without changing the score profile.
2. Otherwise, no submission is warranted: the remaining frontier ablations (A3/A4/A5) are homogeneous
   intermediates, and our own line has no candidate passing the 3-well gate.
3. Quota discipline unchanged: 5/day available, spend only on non-homogeneous, information-bearing runs.
