# Status — 2026-07-28

Live-refreshed **10:18 UTC**. Deadline **2026-08-05 23:59 UTC** → **8.57 days**. Quota **0/5 today**
(last submission 2026-07-26). No Kaggle kernel running, no local job running, branch in sync with
`juktong` (0 unpushed). Autopilot runner alive (pid 278701, 15-min loop).

## Board (unchanged since 2026-07-26)

| ref | public | side | family | provenance | OOF |
|---|---|---|---|---|---|
| `54922806` | **6.563** | teammate | frontier (overlap-ON) | public-derived | no |
| `54968060` | 6.643 | ours | frontier (overlap-OFF, validated no-retrieval) | public-derived | no |
| `54896975` | 6.669 | teammate | frontier (overlap-ON) | public-derived | no |
| `54923144` | 6.678 | teammate | frontier (GR-sigma) | public-derived | no |
| `54990075` | 6.690 | ours | frontier (SP45-only) | public-derived | no |
| `54844628` | **7.891** | ours | **honest** (DWT+PF+structural field) | **fully owned** | **yes** (760-well nested) |
| `54878409` | 7.953 | ours | honest variant | fully owned | excluded (measured regression) |

## Final-2 recommendation — all three criteria converge

```
[score-first]      54922806 (6.563) + 54844628 (7.891)   worst 6.643  mean 6.603
[diversity-first]  54922806 (6.563) + 54844628 (7.891)   worst 6.643  family_diversity 1
[provenance-first] 54922806 (6.563) + 54844628 (7.891)   worst 6.643  mean 6.603
```

Nine pairs tie within 0.01 on worst case, so score alone does not pick a unique pair; diversity and
provenance both break the tie the same way. No criterion prefers a second frontier candidate in slot 2
(family diversity 0, identical worst case), while `54844628` supplies independent-failure insurance at no
modelled cost. Unchanged by this round's work.

## This round (2026-07-28): 7 tasks, 0 submissions, 0 quota

| task | outcome |
|---|---|
| `live_refresh_and_decision` | state refreshed, queue advanced |
| `g35_honest_prefix_calibration` | **closed on evidence** — a same-run confound had inflated the signal 3–4×; honest pairing leaves 3.5% / 0.5% of toe-bias variance |
| `g13_dependency_provenance_audit` | 9 attached datasets narrow to **1** prediction-affecting third-party dependency (`fleongg/rogii-claude-models-pub`) |
| `g32_learned_alignment_smoke` | **first positive in the alignment line** — scorer AUC 0.7242 vs NCC 0.5010; its DP beats flat-anchor 12.527 vs 13.103; still ~42% above deployed ~8.86, so no submission |
| `g33_multi_hypothesis_smoke` | **closed** — K=1 worse than the flat baseline; achievable margin insufficient |
| `frontier_variant_matrix_lite` | **correction issued** (below); both live axes HOLD |
| `new_direction_search` | 5 measurements closed 3 lines; 6 directions queued at 110–160 |

### The correction that matters most

The PF **bimodal branch hedge is the largest single post-SP45 effect** — +2.0 ft on all 4,301 rows of
well `00e12e8b` (separation 29.44 ft; the other two wells are skipped). The 2026-07-25 A5 note recorded it
as contributing zero, because it compared `before_branch_hedge` with `before_model_package` — two files
that are identical but **both upstream of the hedge**, so the comparison could not detect it. A5, G1.3,
G2.1 and the final-slot package are corrected in place.

Knock-on: G1.3's ≈0.047 (`54990075` 6.690 vs `54968060` 6.643) spans **two** active stages — the
learned-trajectory blend *and* the bimodal hedge — so it is a **joint upper bound**, not a clean
measurement of the `fleongg` dataset. The dependency count and the slot recommendation are unaffected.

### Lines closed on evidence this round

1. Honest prefix calibration (G3.5) — same-run confound.
2. Multi-hypothesis trajectory (G3.3) — K=1 worse than flat baseline.
3. Single-dip geometric reparametrization — oracle 7.65 vs honest prefix-fit 80.29 vs flat anchor 15.91.
4. Train-only PNG images as an input modality — 773 train PNGs, **0 test PNGs**, content is a CSV render.
5. Exact typewell grouping — 752 distinct typewell files over 773 wells; **0/3 test wells match**.

## Queue

```
 100 status_summary_for_user            in_progress  (this task)
 110 n4_conformal_well_uncertainty      queued   split conformal on the banked OOF; minutes; decision support
 120 n2_increment_structural_field      queued   the ONLY queued item with a submission path
 130 n1_geometry_bounded_alignment      queued   geometry-derived admissible band on the G3.2 DP
 140 n3_multiscale_gr_matching          queued   per-level DWT scorer AUC vs G3.2's 0.7242
 150 n6_public_solution_audit           queued   methods only; no external data pending rule confirmation
 160 n5_typewell_fingerprint_families   queued   time-boxed 30 min
```

All six run on CPU with no quota cost. **N2** is the one to watch: it swaps the deployed structural
field's interpolated quantity from the TVT level to the heel-referenced increment, attacking the drift
component that holds the dominant share of the residual (slope std 13.28 ft vs level std 4.72 ft). It
must still clear the 3-well gate and the submit gate before any submission.

## Submission posture

20 submissions to date; none since 2026-07-26. At 5/day (team-wide) and 8.57 days left there is ample
quota, so the constraint is **information value per slot**, not slot count. No candidate currently clears
the submit gate: the frontier variant axes are either sub-noise (bimodal fires on 1 of 3 wells, under the
~0.115 config-variance floor) or would re-confirm an already-measured result (prefix-aggressive vs G3.5).

## What the autopilot does next

Runs `n4_conformal_well_uncertainty` (110), then `n2_increment_structural_field` (120). No submission is
planned until N2 produces a candidate that passes the 3-well gate.
